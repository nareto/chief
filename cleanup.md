Implement all open bd tickets linked to `chief-7l2` in this order: `chief-awp`, `chief-bkk`, `chief-jgm`, `chief-vod`, `chief-itt`, `chief-4ns`, then close `chief-7l2`; keep scope Rust-only (no frontend).
For each ticket: set `in_progress`, implement, run `cargo test`, commit, close the ticket, and at the end push all commits and provide a short handoff with ticket IDs, commits, and tests run.
