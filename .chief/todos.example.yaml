todos:
  - priority: 10
    todo: Add rate limiting to the /api/users endpoint
    # Used by both flows. single_prompt runs only the suites listed here.
    test_suites:
      - backend
    expectations: Requests beyond 100/minute should return 429 Too Many Requests
    status: pending
    done_at_commit: null

  - priority: 5
    todo: Fix login redirect bug when session expires
    # Used by both flows. single_prompt runs only the suites listed here.
    test_suites:
      - backend
      - frontend
    expectations: User should be redirected to login page with a 'session expired' message, not a blank screen
    status: pending
    done_at_commit: null

  - priority: 3
    todo: Update README with Docker setup instructions
    # Used by both flows. single_prompt runs only the suites listed here.
    test_suites: []
    expectations: README should include docker-compose commands and environment variable configuration
    status: pending
    done_at_commit: null
